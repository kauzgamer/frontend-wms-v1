# 🚀 Configuração de Ambiente - Render Frontend WMS
# Copie e cole estas variáveis no dashboard do Render

# API Configuration
VITE_API_URL=https://backend-wms-fdsc.onrender.com/api

# Application Settings
VITE_APP_NAME=WMS System
VITE_APP_VERSION=1.0.0

# Environment
NODE_ENV=production

# JWT Configuration (opcional)
VITE_JWT_ACCESS_EXPIRES=15m
VITE_JWT_REFRESH_EXPIRES=7d

# Development fallback (não usado em produção)
VITE_USE_REMOTE_API=false
